.topbar
  .toggle
    ion-icon(name='menu-outline')
  .search
    label
      ion-icon(name='search-outline')
      input(type='text' placeholder='Search here')
  .user
    a(href=`${prefixAdmin}/accounts/read/${user.id}`)
      img(src=user.avatar alt=role.title)
